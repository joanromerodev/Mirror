import UserContainer from "./components/UserContainer"

function App() {

  return (
    // This is the only component that is rendered in the App component, it will render the logic of the application
    <UserContainer />
  )
}

export default App
